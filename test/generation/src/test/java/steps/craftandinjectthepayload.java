package steps;
import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;


// Develop the payload to be executed in the higher privileged zone in the user's browser. Inject the payload and attempt to lure the victim (if possible) into executing the functionality which unleashes the payload.

public class craftandinjectthepayload {
@Given("prepare to Craft and inject the payload")
public void precraftandinjectthepayload(){
// No specialized equipment is needed
// The target must be using a zone-aware browser.
}
@When("Try to Craft and inject the payload")
public void trycraftandinjectthepayload(){
//  Try one of the following techniques :
   //1.  The attacker makes it as likely as possible that the vulnerable functionality into which he has injected the payload has a high likelihood of being used by the victim.
   //2.  Leverage cross-site scripting vulnerability to inject payload.
}@Then("Assert the success of Craft and inject the payload")
public void asscraftandinjectthepayload(){
//  Assert one of the following indications :
}
}