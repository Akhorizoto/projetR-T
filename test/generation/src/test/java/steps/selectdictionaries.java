package steps;
import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;


// Pick the dictionaries to be used in the attack (e.g. different languages, specific terminology, etc.)

public class selectdictionaries {
@Given("prepare to Select dictionaries")
public void preselectdictionaries(){
// A machine with sufficient resources for the job (e.g. CPU, RAM, HD). Applicable dictionaries are required. Also a password cracking tool or a custom script that leverages the dictionary database to launch the attack.
// The system does not implement an effective password throttling mechanism.
}
@When("Try to Select dictionaries")
public void tryselectdictionaries(){
//  Try one of the following techniques :
   //1.  Select dictionary based on particular users' preferred languages.
   //2.  Select dictionary based on the application/system's supported languages.
}@Then("Assert the success of Select dictionaries")
public void assselectdictionaries(){
//  Assert one of the following indications :
}
}