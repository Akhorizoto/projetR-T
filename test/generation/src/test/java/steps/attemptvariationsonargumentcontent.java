package steps;
import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;


// Possibly using an automated tool, the attacker will perform injection variations of the arguments.

public class attemptvariationsonargumentcontent {
@Given("prepare to 1. Attempt variations on argument content")
public void preattemptvariationsonargumentcontent(){
// Ability to communicate synchronously or asynchronously with server. Optionally, ability to capture output directly through synchronous communication or other method such as FTP.
// Software must allow for unvalidated or unfiltered input to be executed on operating system shell, and, optionally, the system configuration must allow for output to be sent back to client.
}
@When("Try to 1. Attempt variations on argument content")
public void tryattemptvariationsonargumentcontent(){
//  Try one of the following techniques :
   //1.  Use a very large list of probe strings in order to detect if there is a positive result, and, what type of system has been targeted (if obscure).
   //2.  Use a proxy tool to record results, error messages and/or log if accessible.
}@Then("Assert the success of 1. Attempt variations on argument content")
public void assattemptvariationsonargumentcontent(){
//  Assert one of the following indications :
}
}