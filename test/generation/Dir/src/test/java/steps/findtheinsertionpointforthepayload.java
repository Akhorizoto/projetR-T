package steps;
import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;


// The attacker first needs to find some system functionality or possibly another weakness in the system (e.g. susceptibility to cross site scripting) that would provide the attacker with a mechanism to deliver the payload (i.e. the code to be executed) to the user. The location from which this code is executed in the user's browser needs to be within the local machine zone.

public class findtheinsertionpointforthepayload {
@Given("prepare to Find the insertion point for the payload")
public void prefindtheinsertionpointforthepayload(){
// No specialized equipment is needed
// The target must be using a zone-aware browser.
}
@When("Try to Find the insertion point for the payload")
public void tryfindtheinsertionpointforthepayload(){
//  Try one of the following techniques :
   //1.  Finding weaknesses in functionality used by both privileged and unprivileged users.
}@Then("Assert the success of Find the insertion point for the payload")
public void assfindtheinsertionpointforthepayload(){
//  Assert one of the following indications :
}
}